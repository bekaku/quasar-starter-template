// Import the SSR app that Quasar has already built.

// eslint-disable-next-line antfu/no-import-dist
import SSRApp from '../dist/ssr/index.js';

// Export to Vercel for further processing.
export default SSRApp;
